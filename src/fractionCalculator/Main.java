package src.fractionCalculator;

public class Main {
    
    public static void main (String[] args) {
        // new Main(); // Runs everything in `public Main()`.
        new CalculatorGUI(500, 500);
    }
}
